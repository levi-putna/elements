{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "sidebar-upcoming",
  "type": "registry:ui",
  "description": "Sidebar schedule components for the application shell. SidebarSchedule shows today's timed slots in a vertical strip beneath navigation. SidebarUpcoming is a low-key multi-day list (collapsed by default with an upcoming count). Both hide when the sidebar collapses to icons.",
  "dependencies": ["date-fns", "lucide-react"],
  "registryDependencies": ["utils", "sidebar"],
  "files": [
    {
      "path": "registry/sidebar-upcoming/sidebar-upcoming.tsx",
      "target": "components/ui/sidebar-upcoming.tsx",
      "type": "registry:ui"
    }
  ]
}
